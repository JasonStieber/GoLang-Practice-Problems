package ch4

func squashSpaces(a string) string {
	// s := ""
	// lastRune rune("")
	// for i, r := range {
	// 	if !unicode.IsSpace(r) || i == 0 || (unicode.IsSpace(r) && !unicode.IsSpace(lastRune)){
	// 		s += a[i]
	// 	}
	// 	lastRune = r
	// }
	// return s
	panic("Do this problem")
	return ""
}
